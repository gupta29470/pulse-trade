<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app talks only to the local development backend. INTERNET is the only
         network permission it needs; there is no exchange SDK and no credentials. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="PulseTrade"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Links are routed by the app itself, so the engine's own
                 deep-link handling is switched off. Left on, both mechanisms
                 act on one intent and a link the app does not recognise is
                 routed as a raw path instead of being ignored. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links, two shapes.
                 The custom scheme is what `adb` and a browser dispatch straight to
                 the app. A chat client will not dispatch an unknown scheme — Slack
                 keeps the tap in its own view — so the https app link is the one a
                 link pasted into a message needs. Android only opens the app for it
                 after verifying that the host belongs to the app, which the backend
                 answers with /.well-known/assetlinks.json; without that file the
                 link stays in the browser. The path prefixes keep the API URLs that
                 share the same host out of the app. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="pulsetrade"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="pulse-trade-backend.onrender.com"
                    android:pathPrefix="/market"/>
                <data
                    android:scheme="https"
                    android:host="pulse-trade-backend.onrender.com"
                    android:pathPrefix="/watchlist"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
