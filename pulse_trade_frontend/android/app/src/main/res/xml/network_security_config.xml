<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext policy for the local development backend.

  The backend serves plain HTTP and WS on the developer machine, so cleartext has
  to be permitted for exactly those hosts and nothing else. A blanket
  usesCleartextTraffic="true" would silently allow every plaintext request the app
  ever makes, which is the wrong default to bake into a build.

  Emulator loopback is 10.0.2.2 (the host's 127.0.0.1). For a physical device on
  the same LAN, add the machine's address as another domain entry, for example:

      <domain includeSubdomains="false">192.168.1.42</domain>
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
